Django>=4.2.0,<5.0
# libtorrent alternative - use one of these options:
# Option 1: Try libtorrent package (alternative name)
libtorrent
# Option 2: If above fails, comment out libtorrent and use requests for basic functionality
# requests>=2.31.0
