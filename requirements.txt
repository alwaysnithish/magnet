# Core Django
Django>=5.0.0,<6.0.0

# WSGI Server for production
gunicorn>=21.2.0

# LibTorrent - try these options in order if one fails:
python-libtorrent>=2.0.0
# Alternative names if above fails:
# libtorrent
# libtorrent-python

# Additional utilities
whitenoise>=6.5.0  # For serving static files in production
